#!/usr/bin/env bash
# dotty guard — pre-commit hook that blocks sensitive content.
# Installed by `dotty guard`. Do not edit directly.

set -euo pipefail

dotty_command=__DOTTY_COMMAND_LITERAL__
exec "$dotty_command" guard-check --staged
